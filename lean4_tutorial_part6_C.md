# Lean4 완전 정복 가이드 — 제6-C편

## **함수의 증가**(Growth of Functions)와 **Big-O 표기법**(Big-O Notation) 완전 정복

> **교재**: Kenneth H. Rosen, "Discrete Mathematics and Its Applications" 8판  
> **범위**: 3.2절 함수의 증가  
> **선수 학습**: 제6-A편(알고리즘 기초), 제6-B편(정렬과 탐색)  
> **참고**: 『Mathematics in Lean』 Chapter 12.2.3 Asymptotic comparisons

---

## 6C.0 이 장의 목표

이 장에서 다루는 핵심 내용은 다음과 같다:

1. **정리**(theorem)와 **보조정리**(lemma)가 무엇인지, 둘의 관계는 어떠한지
2. **조건문**(→, if)과 **쌍조건문**(↔, if and only if)의 차이
3. **치환**(substitution, rewrite)이 증명에서 어떻게 사용되는지
4. **Big-O 표기법**이 무엇인지, 왜 필요한지
5. **증인**(witness)의 개념: C와 k를 찾아서 부등식을 만족시키는 것
6. Lean4에서 이 모든 것을 코드로 구현하는 방법

이 장은 수학을 처음 배우는 학생도 이해할 수 있도록, 가장 기초적인 것부터 차근차근 올라간다.

---

## 6C.1 Lean4에서 **정리**(Theorem)란 무엇인가?

### 6C.1.1 일상적 의미에서의 정리

우리가 일상에서 "정리"라고 부르는 것은 **참이라고 증명된 사실**을 말한다. 예를 들어 "피타고라스 정리"는 직각삼각형에서 빗변의 제곱이 나머지 두 변의 제곱의 합과 같다는 사실이다. 이것은 수천 년 전에 증명되었으며, 그래서 "정리"라고 부른다.

수학에서 정리가 되려면 반드시 **증명**(proof)이 있어야 한다. 증명 없이 "아마 참일 것이다"라고 추측하는 것은 **추측**(conjecture)이라고 부르며, 아직 정리가 아니다.

### 6C.1.2 Lean4에서의 정리

Lean4에서 `theorem`은 **이름이 붙은 증명**이다. 한 번 증명하면, 그 이름으로 다른 곳에서 재사용할 수 있다.

```lean
-- 가장 간단한 정리: "True는 참이다"
theorem my_first_theorem : True := trivial
```

이 코드의 의미를 한 줄씩 분석한다:

| 부분 | 의미 |
|------|------|
| `theorem` | "지금부터 정리를 선언한다"는 키워드 |
| `my_first_theorem` | 이 정리의 이름 (나중에 이 이름으로 호출 가능) |
| `:` | "이 정리가 증명하는 것은..." |
| `True` | 증명할 명제 (여기서는 `True`, 즉 "참") |
| `:=` | "그 증명은 다음과 같다" |
| `trivial` | 실제 증명 (True는 너무 자명하므로 `trivial`로 끝) |

### 6C.1.3 조금 더 의미 있는 정리

```lean
-- "0은 0과 같다"는 정리
theorem zero_eq_zero : 0 = 0 := rfl
```

여기서 `rfl`은 **반사성**(reflexivity)의 약자로, "같은 것은 같다"는 증명이다. `0 = 0`은 양쪽이 완전히 똑같으므로, `rfl` 하나로 증명이 끝난다.

```lean
-- "1 + 1 = 2"라는 정리
theorem one_plus_one : 1 + 1 = 2 := rfl
```

Lean4는 `1 + 1`을 계산하면 `2`가 된다는 것을 알고 있으므로, 역시 `rfl`로 끝난다.

### 6C.1.4 정리를 재사용하기

정리에 이름을 붙이는 진짜 이유는 **재사용**이다.

```lean
-- 정리: 모든 자연수 n에 대해 n + 0 = n
theorem add_zero_right (n : Nat) : n + 0 = n := by omega

-- 위 정리를 다른 곳에서 사용
theorem add_zero_twice (n : Nat) : (n + 0) + 0 = n := by
  rw [add_zero_right]   -- 첫 번째 + 0을 제거
```

`rw [add_zero_right]`는 **치환**(substitution)이다. 앞에서 증명한 정리를 현재 목표에 "대입"해서 식을 바꾸는 것이다. 이것이 바로 **정리의 힘**이다 — 한 번 증명하면 여러 번 쓸 수 있다.

---

## 6C.2 **보조정리**(Lemma)란 무엇인가?

### 6C.2.1 보조정리의 일상적 의미

**보조정리**(lemma)는 **큰 정리를 증명하기 위해 먼저 증명하는 작은 정리**이다.

비유하자면:
- **정리**(theorem) = 완성된 건물
- **보조정리**(lemma) = 건물을 짓기 위한 벽돌 하나하나

건물을 한 번에 짓는 것은 너무 복잡하다. 그래서 먼저 벽돌(보조정리)을 하나씩 만들고, 그것들을 쌓아서 건물(정리)을 완성한다.

### 6C.2.2 Lean4에서의 보조정리

Lean4에서 `lemma`와 `theorem`은 **기술적으로 완전히 동일**하다. 차이는 **의도**에 있다:

| 키워드 | 의도 | 비유 |
|--------|------|------|
| `theorem` | 최종적으로 보여주고 싶은 중요한 결과 | 완성된 건물 |
| `lemma` | 큰 정리를 돕는 보조적 사실 | 건물의 벽돌 |

```lean
-- 보조정리: 어떤 수에 1을 더하면 원래보다 크다
lemma succ_gt_self (n : Nat) : n + 1 > n := by omega

-- 정리: 어떤 수에 2를 더하면 원래보다 크다
-- (위의 보조정리를 활용하여 증명)
theorem add_two_gt_self (n : Nat) : n + 2 > n := by
  have h1 : n + 1 > n := succ_gt_self n            -- 보조정리 사용!
  have h2 : n + 2 > n + 1 := succ_gt_self (n + 1)  -- 보조정리 재사용!
  omega
```

위 예제에서 `succ_gt_self`이라는 보조정리를 두 번 사용해서 `add_two_gt_self`이라는 정리를 증명했다. 이것이 보조정리의 역할이다.

### 6C.2.3 정리와 보조정리의 관계 요약

```
정리(theorem) = 보조정리1 + 보조정리2 + ... + 보조정리n + 추가 논증
```

**핵심**: Lean4에게 `theorem`과 `lemma`는 같은 것이다. 어떤 키워드를 쓸지는 순전히 인간의 조직 습관이다. 하지만 코드를 읽는 사람에게는 "이것은 보조 단계"인지 "이것이 최종 결과"인지 구분하는 데 도움이 된다.

---

## 6C.3 **조건문**(→)과 **쌍조건문**(↔)의 차이

이것은 매우 중요한 주제이다. 많은 학생이 "→"와 "↔"를 혼동하므로, 아주 자세히 설명한다.

### 6C.3.1 **조건문**(→): "만약 ... 이면 ..."

**조건문**(implication)은 영어로 "if P then Q"이며, 기호로는 `P → Q`이다.

일상 예시:
- "만약 비가 오면(P), 땅이 젖는다(Q)" → `비가_옴 → 땅이_젖음`
- "만약 시험에 합격하면(P), 졸업할 수 있다(Q)" → `합격 → 졸업_가능`

**핵심**: `P → Q`는 **한쪽 방향**만 보장한다. 비가 오면 땅이 젖지만, 땅이 젖었다고 반드시 비가 온 것은 아니다.

### 6C.3.2 Lean4에서 조건문 증명: `intro`

```lean
-- P → Q를 증명하려면: P가 참이라고 가정하고, Q를 보인다
theorem my_impl (n : Nat) : n > 5 → n > 3 := by
  intro h       -- h : n > 5 라고 가정
  omega         -- n > 5이면 n > 3은 자동
```

`intro h`는 "P가 참이라고 가정하고, 그 가정을 h라고 부르자"는 뜻이다.

### 6C.3.3 **쌍조건문**(↔): "... 일 때 그리고 그때에만 ..."

**쌍조건문**(biconditional)은 영어로 "P if and only if Q"이며, 기호로는 `P ↔ Q`이다. 줄여서 **iff** 라고도 한다.

`P ↔ Q`의 의미: **P → Q이고 동시에 Q → P**이다. 양쪽 방향이 모두 성립한다.

### 6C.3.4 조건문 vs 쌍조건문 비교

| 구분 | **조건문** `→` | **쌍조건문** `↔` |
|------|-------------|---------------|
| 영문 | if P then Q | P if and only if Q |
| 한국어 | P이면 Q | P일 때 그리고 그때에만 Q |
| 방향 | **한쪽** (P에서 Q로만) | **양쪽** (P↔Q) |
| 포함 관계 | `P → Q` 하나 | `(P → Q) ∧ (Q → P)` 둘 |
| 비유 | 일방통행 도로 | 양방향 도로 |
| Lean4 증명 | `intro` 한 번 | `constructor` 후 각 방향 증명 |

### 6C.3.5 Lean4에서 쌍조건문 증명: `constructor`

```lean
-- n > 0 ↔ n ≥ 1 (자연수에서)
theorem pos_iff_ge_one (n : Nat) : n > 0 ↔ n ≥ 1 := by
  constructor           -- 두 방향으로 나눈다
  · intro h             -- 방향 1: n > 0 → n ≥ 1
    omega
  · intro h             -- 방향 2: n ≥ 1 → n > 0
    omega
```

`constructor`는 `↔`을 두 개의 `→`로 분리한다.

### 6C.3.6 `.mp`와 `.mpr`로 방향 추출

쌍조건문 `h : P ↔ Q`가 있을 때:
- `h.mp` : **정방향** P → Q (mp = modus ponens)
- `h.mpr` : **역방향** Q → P (mpr = modus ponens reverse)

```lean
example (P Q : Prop) (h : P ↔ Q) (hp : P) : Q := h.mp hp
example (P Q : Prop) (h : P ↔ Q) (hq : Q) : P := h.mpr hq
```

### 6C.3.7 `rw`로 쌍조건문 치환

쌍조건문의 강력한 사용법: `rw`로 식을 **치환**할 수 있다.

```lean
example (a b : Nat) (h : a = 0 ↔ b = 0) (hb : b = 0) : a = 0 := by
  rw [h]          -- 목표가 a = 0 에서 b = 0으로 바뀜
  exact hb
```

**주의**: `rw`는 `↔`에 대해서도 작동하지만, `→`에 대해서는 작동하지 않는다. 이것이 `→`와 `↔`의 실질적 차이 중 하나이다.

---

## 6C.4 **치환**(Substitution)과 `rw` 전술 복습

### 6C.4.1 치환이란?

**치환**(substitution)은 "같은 것은 바꿔 쓸 수 있다"는 원리이다. a = b라는 것을 알면, a가 등장하는 곳에 b를 쓸 수 있다. Lean4에서 이 원리를 구현하는 전술이 `rw`(rewrite)이다.

### 6C.4.2 `rw`의 방향

| 사용법 | 효과 |
|--------|------|
| `rw [h]` | h의 좌변 → 우변으로 치환 |
| `rw [← h]` | h의 우변 → 좌변으로 치환 |

```lean
example (a b c : Nat) (h1 : a = b) (h2 : b = c) : a = c := by
  rw [h1, h2]    -- a→b, b→c 를 순서대로 적용
```

---

## 6C.5 연습 세트 1: 정리, 보조정리, 조건문, 쌍조건문

### 연습 6C.1: 첫 번째 정리 (괄호 채우기)

```lean
theorem three_plus_four : 3 + 4 = ▢ := ▢
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem three_plus_four : 3 + 4 = 7 := rfl
```

</details>

### 연습 6C.2: 보조정리 사용 (괄호 채우기)

```lean
lemma lem_add_comm (a b : Nat) : a + b = b + a := by omega

theorem comm_ex : 5 + 3 = 3 + 5 := by
  exact lem_add_comm ▢ ▢
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem comm_ex : 5 + 3 = 3 + 5 := by
  exact lem_add_comm 5 3
```

</details>

### 연습 6C.3: 조건문 증명 (sorry)

```lean
theorem pos_ne_zero (n : Nat) : n > 0 → n ≠ 0 := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem pos_ne_zero (n : Nat) : n > 0 → n ≠ 0 := by
  intro h
  omega
```

</details>

### 연습 6C.4: 쌍조건문 증명 (sorry)

```lean
theorem even_iff (n : Nat) : n % 2 = 0 ↔ 2 ∣ n := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem even_iff (n : Nat) : n % 2 = 0 ↔ 2 ∣ n := by
  constructor
  · intro h
    omega
  · intro h
    omega
```

</details>

### 연습 6C.5: .mp 사용 (괄호 채우기)

```lean
example (P Q : Prop) (h : P ↔ Q) (hp : P) : Q := by
  exact h.▢ ▢
```

<details>
<summary>💡 답 보기</summary>

```lean
example (P Q : Prop) (h : P ↔ Q) (hp : P) : Q := by
  exact h.mp hp
```

</details>

### 연습 6C.6: rw + ↔ (sorry)

```lean
theorem rw_iff_ex (a b : Nat) (h : a = 0 ↔ b = 0) (hb : b = 0) : a = 0 := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem rw_iff_ex (a b : Nat) (h : a = 0 ↔ b = 0) (hb : b = 0) : a = 0 := by
  rw [h]
  exact hb
```

</details>

### 연습 6C.7: lemma → theorem 패턴 (sorry)

```lean
lemma my_lem (a : Nat) (h : a % 2 = 0) : a % 2 = 0 := by sorry

theorem my_thm (a : Nat) (h : a % 2 = 0) : a % 2 = 0 := by sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
lemma my_lem (a : Nat) (h : a % 2 = 0) : a % 2 = 0 := exact h

theorem my_thm (a : Nat) (h : a % 2 = 0) : a % 2 = 0 := my_lem a h
```

</details>

---

## 6C.6 **Big-O 표기법**(Big-O Notation)이란?

### 6C.6.1 왜 Big-O가 필요한가?

두 알고리즘이 같은 문제를 푼다고 하자. 어떤 것이 더 "빠른"가를 비교하고 싶다. 하지만 "빠르다"는 것은 컴퓨터 종류, 프로그래밍 언어, 입력 크기 등 여러 요인에 달렸다.

Big-O 표기법은 이런 세부사항을 무시하고, **입력 크기가 커질 때 함수가 얼마나 빨리 증가하는가**만 비교한다.

### 6C.6.2 Big-O의 직관

`f(x)가 O(g(x))이다`의 의미:

> f(x)는 결국(x가 충분히 크면) g(x)의 **상수배**보다 작거나 같아진다.

**예시**:
- `3x + 5`는 `O(x)` — 상수와 작은 항을 무시하면 x 정도로 자란다
- `x² + 2x + 1`은 `O(x²)` — 최고차항이 x²
- `7x²`은 `O(x²)` — 상수 7은 무시

### 6C.6.3 Big-O의 정식 정의 (교재 정의 1)

> **정의**: f와 g가 정수 집합(또는 실수 집합)에서 실수로의 함수라 하자. x > k일 때 항상 |f(x)| ≤ C|g(x)|를 만족하는 **상수** C와 k가 존재하면, f(x)는 O(g(x))라고 정의한다.

기호로:

```
f(x) ∈ O(g(x))  ⟺  ∃ C > 0, ∃ k, ∀ x > k, |f(x)| ≤ C · |g(x)|
```

| 요소 | 이름 | 역할 |
|------|------|------|
| C | **상수 배수** | f(x)가 g(x)의 C배 이하 |
| k | **임계점**(threshold) | x가 k보다 클 때부터 성립 |
| C, k | **증인**(witness) | Big-O 관계를 "증명"하는 구체적 숫자 |

### 6C.6.4 Lean4에서 Big-O 정의

```lean
-- 자연수 버전 (절댓값 불필요)
def IsBigO (f g : Nat → Nat) : Prop :=
  ∃ C k, ∀ x, x > k → f x ≤ C * g x
```

이 정의를 한 줄씩 읽으면:

| 부분 | 의미 |
|------|------|
| `∃ C k` | "어떤 상수 C와 k가 존재해서..." |
| `∀ x` | "모든 x에 대해..." |
| `x > k →` | "x가 k보다 크면..." |
| `f x ≤ C * g x` | "f(x)는 C·g(x) 이하이다" |

---

## 6C.7 교재 예제를 Lean4로

### 6C.7.1 예제 1: f(x) = x² + 2x + 1은 O(x²)

**교재 풀이** (p.245): x > 1이면 x < x²이고 1 < x²이므로 x² + 2x + 1 ≤ 4x². 증인: C = 4, k = 1.

```lean
def f_ex1 (x : Nat) : Nat := x ^ 2 + 2 * x + 1
def g_ex1 (x : Nat) : Nat := x ^ 2

theorem ex1_big_o : IsBigO f_ex1 g_ex1 := by
  use 4, 1                      -- 증인: C = 4, k = 1
  intro x hx                    -- 임의의 x > 1
  unfold f_ex1 g_ex1            -- 함수 정의 펼치기
  nlinarith [sq_nonneg x]       -- 비선형 산술 자동 증명
```

**코드 해설**:

| 코드 | 의미 |
|------|------|
| `use 4, 1` | "증인으로 C = 4, k = 1을 선택한다" |
| `intro x hx` | "임의의 x를 잡는다. hx : x > 1" |
| `unfold f_ex1 g_ex1` | 함수 정의를 실제 식으로 펼친다 |
| `nlinarith [sq_nonneg x]` | 비선형 산술로 부등식 증명 (x² ≥ 0 힌트) |

### 6C.7.2 예제 2: 7x²은 O(x³)

**교재 풀이** (p.246): x > 7이면 7x² < x³. 증인: C = 1, k = 7.

```lean
def f_ex2 (x : Nat) : Nat := 7 * x ^ 2
def g_ex2 (x : Nat) : Nat := x ^ 3

theorem ex2_big_o : IsBigO f_ex2 g_ex2 := by
  use 1, 7
  intro x hx
  unfold f_ex2 g_ex2
  nlinarith [sq_nonneg x]
```

### 6C.7.3 예제 3: n²은 O(n)이 아니다

**교재 풀이** (p.248): n² ≤ Cn이면 n ≤ C인데, n은 얼마든지 크게 할 수 있으므로 모순.

```lean
theorem n_sq_not_O_n : ¬ IsBigO (fun x => x ^ 2) (fun x => x) := by
  intro ⟨C, k, h⟩
  have hx := h (C + k + 1) (by omega)
  nlinarith [sq_nonneg (C + k + 1)]
```

**해설**: `¬`(부정)을 증명하려면, Big-O라고 가정한 후 모순을 유도한다. x = C + k + 1을 대입하면 (C+k+1)² ≤ C·(C+k+1)이 되어야 하는데, 이는 C+k+1 ≤ C를 의미하므로 모순이다.

### 6C.7.4 예제 4: x³은 O(7x²)가 아니다

**교재** (p.249): 비슷한 논증으로 x³ ≤ C·7x²이면 x ≤ 7C인데, x를 7C보다 크게 잡으면 모순.

```lean
theorem x3_not_O_7x2 : ¬ IsBigO (fun x => x ^ 3) (fun x => 7 * x ^ 2) := by
  intro ⟨C, k, h⟩
  have hx := h (7 * C + k + 1) (by omega)
  nlinarith [sq_nonneg (7 * C + k + 1)]
```

---

## 6C.8 Big-O에서 자주 쓰는 참조 함수들

교재 (p.251)의 증가 순서:

```
1 ≪ log n ≪ n ≪ n log n ≪ n² ≪ 2ⁿ ≪ n!
```

| 함수 | 이름 | 예시 알고리즘 |
|------|------|------------|
| 1 | **상수**(constant) | 배열의 첫 원소 접근 |
| log n | **로그**(logarithmic) | **이진 탐색** |
| n | **선형**(linear) | **선형 탐색** |
| n log n | **선형로그**(linearithmic) | **병합 정렬** |
| n² | **이차**(quadratic) | **버블 정렬** |
| 2ⁿ | **지수**(exponential) | 부분집합 나열 |
| n! | **계승**(factorial) | 순열 나열 |

---

## 6C.9 교재 정리 1: 다항식의 Big-O

**정리 1** (p.249): f(x) = aₙxⁿ + ... + a₁x + a₀이면 f(x)는 O(xⁿ).

직관: **다항식의 증가 속도는 최고차항이 결정한다.** 아무리 낮은 차수의 계수가 크더라도, x가 충분히 커지면 최고차항이 나머지를 압도한다.

```lean
-- 특수 경우: ax² + bx + c는 O(x²)
lemma quadratic_big_o (a b c : Nat) :
    IsBigO (fun x => a * x ^ 2 + b * x + c) (fun x => x ^ 2) := by
  use a + b + c, 1
  intro x hx
  nlinarith [sq_nonneg x]
```

증인: C = a + b + c, k = 1. x > 1이면 x ≤ x²이고 1 ≤ x²이므로 bx ≤ bx², c ≤ cx².

---

## 6C.10 교재 예제 5~6: 급수와 계승

### 6C.10.1 예제 5: 1 + 2 + ... + n은 O(n²)

각 항이 n 이하이므로: 1 + 2 + ... + n ≤ n + n + ... + n = n². 증인: C = 1, k = 1.

```lean
def sumUpTo : Nat → Nat
  | 0 => 0
  | n + 1 => (n + 1) + sumUpTo n

lemma sumUpTo_le_sq (n : Nat) : sumUpTo n ≤ n ^ 2 := by
  induction n with
  | zero => simp [sumUpTo]
  | succ n ih =>
    simp [sumUpTo]
    nlinarith [sq_nonneg n]
```

### 6C.10.2 예제 6: n!은 O(nⁿ), log n!은 O(n log n)

n! = 1·2·...·n ≤ n·n·...·n = nⁿ이므로 n!은 O(nⁿ). 양변에 로그를 취하면 log n! ≤ n log n.

---

## 6C.11 Big-O의 결합 규칙

### 6C.11.1 정리 2: Big-O의 합

**정리 2** (p.253): f₁이 O(g)이고 f₂가 O(g)이면, (f₁ + f₂)도 O(g)이다.

```lean
theorem big_o_add {f₁ f₂ g : Nat → Nat}
    (h₁ : IsBigO f₁ g) (h₂ : IsBigO f₂ g) :
    IsBigO (fun x => f₁ x + f₂ x) g := by
  obtain ⟨C₁, k₁, hf₁⟩ := h₁
  obtain ⟨C₂, k₂, hf₂⟩ := h₂
  use C₁ + C₂, max k₁ k₂
  intro x hx
  have hx₁ : x > k₁ := by omega
  have hx₂ : x > k₂ := by omega
  have := hf₁ x hx₁
  have := hf₂ x hx₂
  nlinarith
```

**해설**: 증인은 C = C₁ + C₂, k = max(k₁, k₂). f₁ x ≤ C₁·g x이고 f₂ x ≤ C₂·g x이므로 f₁ x + f₂ x ≤ (C₁+C₂)·g x.

### 6C.11.2 정리 3: Big-O의 곱

**정리 3** (p.254): f₁이 O(g₁)이고 f₂가 O(g₂)이면, (f₁·f₂)는 O(g₁·g₂)이다.

```lean
theorem big_o_mul {f₁ f₂ g₁ g₂ : Nat → Nat}
    (h₁ : IsBigO f₁ g₁) (h₂ : IsBigO f₂ g₂) :
    IsBigO (fun x => f₁ x * f₂ x) (fun x => g₁ x * g₂ x) := by
  obtain ⟨C₁, k₁, hf₁⟩ := h₁
  obtain ⟨C₂, k₂, hf₂⟩ := h₂
  use C₁ * C₂, max k₁ k₂
  intro x hx
  have hx₁ : x > k₁ := by omega
  have hx₂ : x > k₂ := by omega
  have h1 := hf₁ x hx₁
  have h2 := hf₂ x hx₂
  calc f₁ x * f₂ x
      ≤ (C₁ * g₁ x) * (C₂ * g₂ x) := by nlinarith
    _ = C₁ * C₂ * (g₁ x * g₂ x) := by ring
```

---

## 6C.12 연습 세트 2: Big-O 증명

### 연습 6C.8: 기본 Big-O (괄호 채우기)

```lean
-- f(x) = 3x + 5는 O(x)
theorem ex_3x5 : IsBigO (fun x => 3 * x + 5) (fun x => x) := by
  use ▢, ▢
  intro x hx
  omega
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem ex_3x5 : IsBigO (fun x => 3 * x + 5) (fun x => x) := by
  use 8, 0
  intro x hx
  omega
```

</details>

### 연습 6C.9: 증인 찾기 (sorry)

```lean
-- x + 100은 O(x)
theorem ex_x100 : IsBigO (fun x => x + 100) (fun x => x) := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem ex_x100 : IsBigO (fun x => x + 100) (fun x => x) := by
  use 101, 0
  intro x hx
  omega
```

</details>

### 연습 6C.10: 이차 Big-O (sorry)

```lean
-- 2x² + 3x는 O(x²)
theorem ex_2x2_3x : IsBigO (fun x => 2 * x ^ 2 + 3 * x) (fun x => x ^ 2) := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem ex_2x2_3x : IsBigO (fun x => 2 * x ^ 2 + 3 * x) (fun x => x ^ 2) := by
  use 5, 1
  intro x hx
  nlinarith [sq_nonneg x]
```

</details>

### 연습 6C.11: 상수 함수 Big-O (sorry)

```lean
-- 상수 10은 O(x)
theorem ex_const10 : IsBigO (fun _ : Nat => 10) (fun x => x) := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem ex_const10 : IsBigO (fun _ : Nat => 10) (fun x => x) := by
  use 10, 0
  intro x hx
  omega
```

</details>

### 연습 6C.12: Big-O 아닌 것 증명 (도전)

```lean
-- x³은 O(x²)가 아니다
theorem x3_not_O_x2 : ¬ IsBigO (fun x => x ^ 3) (fun x => x ^ 2) := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem x3_not_O_x2 : ¬ IsBigO (fun x => x ^ 3) (fun x => x ^ 2) := by
  intro ⟨C, k, h⟩
  have hx := h (C + k + 1) (by omega)
  nlinarith [sq_nonneg (C + k + 1)]
```

</details>

### 연습 6C.13: Big-O 합 규칙 적용 (괄호 채우기)

```lean
-- 두 함수가 각각 O(g)이면 합도 O(g)
example (f f' g : Nat → Nat) (h1 : IsBigO f g) (h2 : IsBigO f' g) :
    IsBigO (fun x => f x + f' x) g := by
  exact big_o_add ▢ ▢
```

<details>
<summary>💡 답 보기</summary>

```lean
example (f f' g : Nat → Nat) (h1 : IsBigO f g) (h2 : IsBigO f' g) :
    IsBigO (fun x => f x + f' x) g := by
  exact big_o_add h1 h2
```

</details>

### 연습 6C.14: 교재 연습문제 3 (sorry)

**문제**: "f(x)가 O(g(x))이다"의 정의를 이용하여 x⁴ + 9x³ + 4x + 7이 O(x⁴)임을 보이시오.

```lean
theorem ex3 : IsBigO (fun x => x ^ 4 + 9 * x ^ 3 + 4 * x + 7) (fun x => x ^ 4) := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem ex3 : IsBigO (fun x => x ^ 4 + 9 * x ^ 3 + 4 * x + 7) (fun x => x ^ 4) := by
  use 21, 1
  intro x hx
  nlinarith [sq_nonneg x, sq_nonneg (x^2)]
```

증인: C = 1 + 9 + 4 + 7 = 21, k = 1.

</details>

### 연습 6C.15: 교재 연습문제 9 유형 (sorry)

**문제**: x² + 4x + 17은 O(x³)이지만 x³은 O(x² + 4x + 17)이 아님을 보이시오.

```lean
-- 파트 1: O(x³)임을 보임
theorem ex9_part1 : IsBigO (fun x => x ^ 2 + 4 * x + 17) (fun x => x ^ 3) := by
  sorry

-- 파트 2: 역은 성립하지 않음
theorem ex9_part2 : ¬ IsBigO (fun x => x ^ 3) (fun x => x ^ 2 + 4 * x + 17) := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem ex9_part1 : IsBigO (fun x => x ^ 2 + 4 * x + 17) (fun x => x ^ 3) := by
  use 22, 1
  intro x hx
  nlinarith [sq_nonneg x]

theorem ex9_part2 : ¬ IsBigO (fun x => x ^ 3) (fun x => x ^ 2 + 4 * x + 17) := by
  intro ⟨C, k, h⟩
  -- x = 22*C + k + 17 을 대입하면 모순
  have hx := h (22 * C + k + 17) (by omega)
  nlinarith [sq_nonneg (22 * C + k + 17)]
```

</details>

---

## 6C.13 전술 요약

### 새로운 전술 & 개념

| 전술/개념 | 용도 | 예시 |
|---------|------|------|
| `theorem` | 이름 붙은 증명 선언 | `theorem foo : P := ...` |
| `lemma` | 보조 증명 선언 | `lemma bar : P := ...` |
| `↔` | 쌍조건문 (양방향) | `P ↔ Q` |
| `constructor` | ↔을 두 방향으로 분리 | 목표: `P ↔ Q` |
| `.mp` / `.1` | 정방향 추출 | `h.mp hp` |
| `.mpr` / `.2` | 역방향 추출 | `h.mpr hq` |
| `use` | 존재 증인 제시 | `use 4, 1` |
| `obtain` | 존재 가정 분해 | `obtain ⟨C, k, h⟩ := hyp` |
| `unfold` | 함수 정의 펼치기 | `unfold IsBigO` |
| `nlinarith` | 비선형 산술 증명 | `nlinarith [sq_nonneg x]` |
| `calc` | 계산 체인 | `calc a ≤ b := ... _ ≤ c := ...` |

### 이전 장 전술 (복습)

| 전술 | 최초 등장 |
|------|---------|
| `intro`, `exact` | Part 4 |
| `rw`, `simp` | Part 5 |
| `omega` | Part 4 |
| `induction` | Part 6-B |

---

## 6C.14 핵심 정리 요약

1. **정리**(theorem)와 **보조정리**(lemma)는 Lean4에서 동일하다. 차이는 의도에 있다.

2. **→ vs ↔**:
   - `→`: 한 방향, `intro`로 증명
   - `↔`: 양 방향, `constructor`로 증명, `.mp`/`.mpr`로 추출, `rw`로 치환 가능

3. **Big-O**: ∃ C k, ∀ x > k, f(x) ≤ C·g(x)
   - 증명: `use C, k` → `intro x hx` → 부등식 증명

4. **결합 규칙**: O(g) + O(g) = O(g), O(g₁) × O(g₂) = O(g₁×g₂)

5. **증가 순서**: 1 ≪ log n ≪ n ≪ n log n ≪ n² ≪ 2ⁿ ≪ n!

---

## 다음 편(6-D) 예고

**제6-D편**에서는:
- **Big-Omega**(Ω): 증가의 하한
- **Big-Theta**(Θ): 상한과 하한 동시에
- **알고리즘의 시간복잡도** 분석 (교재 3.3절)
- 최선/평균/최악의 경우

를 다룬다.

---

**(끝)**
