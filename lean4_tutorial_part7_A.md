# Lean4 완전 정복 가이드 — 제7-A편

## **정수론 입문**(Introduction to Number Theory) — **가분성**(Divisibility) 완전 정복

> **교재**: Kenneth H. Rosen, "Discrete Mathematics and Its Applications" 8판, 4.1절  
> **참고**: 『Mathematics in Lean』 Chapter 2.4, Chapter 5 Elementary Number Theory  
> **선수 학습**: 제6편까지의 내용 (기본 전술: `intro`, `exact`, `apply`, `rw`, `simp`, `omega`)

---

## 7A.0 이 장의 목표

이 장에서 배울 내용은 다음과 같다:

1. **가분성**(divisibility)이란 무엇인가 — "a가 b를 나눈다"의 정확한 의미
2. Lean4에서 **나눗셈 기호** `∣`(dvd)를 사용하는 방법
3. **정리**(theorem)와 **보조정리**(lemma)의 관계 — 큰 증명을 작은 조각으로 나누기
4. **조건문**(→, if)과 **쌍조건문**(↔, if and only if)의 차이
5. **치환**(substitution, rewrite)이 증명에서 어떻게 쓰이는지 복습
6. 교재의 예제와 연습문제를 Lean4로 풀기

이 장은 **정수론이 무엇인지 전혀 모르는 학생**도 이해할 수 있도록, 가장 기본적인 것부터 시작한다.

---

## 7A.1 **정수론**(Number Theory)이란?

**정수론**(Number Theory)은 **정수의 성질**을 연구하는 수학의 한 분야이다. 정수란 ..., -3, -2, -1, 0, 1, 2, 3, ...과 같은 수를 말한다. 소수점이 없는 수, 분수가 아닌 수가 정수이다.

정수론에서 가장 기본이 되는 개념이 바로 **가분성**(divisibility)이다. "어떤 수가 다른 수를 나누어 떨어지게 하는가?"라는 질문은 매우 단순해 보이지만, 이 질문에서 출발하여 놀라울 정도로 깊고 아름다운 수학이 전개된다.

---

## 7A.2 **가분성**(Divisibility)이란?

### 7A.2.1 일상적 의미

"12를 3으로 나누면?" → 4가 되고, 나머지가 0이다. 이때 우리는 "3이 12를 **나눈다**"(3 divides 12)라고 말한다.

"12를 5로 나누면?" → 2가 되고, 나머지가 2이다. 나머지가 0이 아니므로 "5는 12를 나누지 **않는다**"(5 does not divide 12)라고 말한다.

### 7A.2.2 교재의 정의 (정의 1)

> **정의 1** (Rosen 4.1절):  
> 만약 a와 b가 **정수**(integer)이고 a ≠ 0일 때, b = ac가 되는 정수 c가 있다면  
> (동등한 다른 표현으로, b/a가 정수라면) 우리는 **a가 b를 나눈다**라고 한다.  
> a가 b를 나누면, a는 b의 **인수**(factor)이거나 **약수**(divisor)이며, b는 a의 **배수**(multiple)이다.  
> a ∣ b 는 a가 b를 나눈다는 것을 표현한 것이다.  
> 만약 a가 b를 나누지 않는다면 a ∤ b로 표현한다.

쉽게 말하면:

| 표현 | 의미 | 예시 |
|------|------|------|
| a ∣ b | "a가 b를 나눈다" = "b를 a로 나누면 나머지가 0" | 3 ∣ 12 (12 = 3 × 4) |
| a ∤ b | "a가 b를 나누지 않는다" = "b를 a로 나누면 나머지 ≠ 0" | 5 ∤ 12 (12 = 5 × 2 + 2) |

**핵심 포인트**: a ∣ b라는 것은 "b = a × (어떤 정수)"가 성립한다는 뜻이다.

### 7A.2.3 예시로 이해하기

| 질문 | 답 | 이유 |
|------|-----|------|
| 3 ∣ 12? | 예 | 12 = 3 × 4 |
| 3 ∣ 7? | 아니오 | 7 = 3 × 2 + 1 (나머지 1) |
| 5 ∣ 0? | 예 | 0 = 5 × 0 |
| 1 ∣ 17? | 예 | 17 = 1 × 17 |
| 7 ∣ 7? | 예 | 7 = 7 × 1 |
| 0 ∣ 5? | 아니오 | 5 = 0 × c를 만족하는 c가 없다 |
| 0 ∣ 0? | 예 | 0 = 0 × (아무 수) |

### 7A.2.4 중요한 사실

- **모든 0이 아닌 정수는 0을 나눈다**: a ≠ 0이면 a ∣ 0 (왜냐하면 0 = a × 0)
- **1은 모든 정수를 나눈다**: 1 ∣ b (왜냐하면 b = 1 × b)
- **모든 정수는 자기 자신을 나눈다**: a ∣ a (왜냐하면 a = a × 1)

---

## 7A.3 Lean4에서 **가분성**(Divisibility) 표현하기

### 7A.3.1 가분성 기호 `∣`

Lean4에서 "a가 b를 나눈다"는 `a ∣ b`로 쓴다.

**주의!** 이 기호는 키보드의 일반적인 `|` (파이프)가 **아니다**. VS Code에서 `\|`로 입력한다. 유니코드 문자 `∣` (U+2223, DIVIDES)이다.

```lean
-- Lean4에서 이 기호를 확인해 보자
#check (3 : ℤ) ∣ 12   -- 3이 12를 나누는가?
```

### 7A.3.2 가분성의 정의 — Lean4 내부

Lean4에서 `a ∣ b`는 내부적으로 다음과 같이 정의되어 있다:

```lean
-- Lean4 내부 정의 (참고용, 직접 작성할 필요 없음)
-- a ∣ b ↔ ∃ c, b = a * c
```

즉, `a ∣ b`라는 것은 "b = a * c를 만족하는 어떤 정수 c가 **존재한다**(∃)"는 뜻이다.

이것은 교재의 정의와 정확히 같다! 교재에서 "b = ac가 되는 정수 c가 있다면"이라고 한 것이 Lean4에서는 `∃ c, b = a * c`가 된다.

### 7A.3.3 가장 간단한 가분성 증명

이제 "3이 12를 나눈다"를 Lean4로 증명해 보자.

```lean
-- 방법 1: norm_num 전술 사용 (자동 계산)
example : (3 : ℤ) ∣ 12 := by norm_num

-- 방법 2: decide 전술 사용 (자연수에서)
example : (3 : ℕ) ∣ 12 := by decide

-- 방법 3: 직접 증인(witness)을 제시
example : (3 : ℤ) ∣ 12 := ⟨4, by ring⟩
```

방법 3을 한 줄씩 분석한다:

| 부분 | 의미 |
|------|------|
| `⟨4, by ring⟩` | "12 = 3 × 4임을 보인다" |
| `4` | **증인**(witness): c = 4 |
| `by ring` | "12 = 3 × 4"라는 등식을 산술적으로 검증 |

`⟨ ⟩`는 **익명 생성자**(anonymous constructor)라 부르며, 존재 명제의 증거를 만들 때 사용한다. `⟨4, by ring⟩`는 "c = 4를 선택하고, 12 = 3 × 4를 ring으로 확인한다"는 뜻이다.

### 7A.3.4 자연수(ℕ) vs 정수(ℤ)

Lean4에서는 **자연수**(ℕ = {0, 1, 2, ...})와 **정수**(ℤ = {..., -2, -1, 0, 1, 2, ...})가 다른 타입이다. 교재에서는 정수를 기본으로 다루지만, Lean4에서는 자연수를 다루는 것이 더 간단한 경우가 많다.

```lean
-- 자연수에서의 가분성
example : (3 : ℕ) ∣ 12 := by norm_num

-- 정수에서의 가분성
example : (3 : ℤ) ∣ 12 := by norm_num

-- 자연수에서: 3은 7을 나누지 않는다
example : ¬ ((3 : ℕ) ∣ 7) := by norm_num
```

이 튜토리얼에서는 주로 **자연수**(ℕ)를 사용한다. 정수가 필요한 경우에는 명시적으로 `ℤ`를 사용할 것이다.

---

## 7A.4 **정리**(theorem)와 **보조정리**(lemma)의 관계

이제 가분성의 기본 성질을 증명하기 전에, **정리**와 **보조정리**의 관계를 확실히 이해하자.

### 7A.4.1 일상적 의미

수학에서 큰 결과를 증명할 때, 처음부터 끝까지 한 번에 증명하기보다 **작은 조각으로 나누어** 증명하는 것이 일반적이다. 이때:

- **정리**(theorem): 중요한 결과. 우리가 최종적으로 증명하고 싶은 것
- **보조정리**(lemma): 정리를 증명하기 위해 먼저 증명하는 작은 사실

예를 들어, "100층 건물을 짓는다"가 정리라면, "1층 기초를 다진다", "10층까지 골조를 세운다" 같은 것이 보조정리이다.

### 7A.4.2 Lean4에서의 `theorem`과 `lemma`

**매우 중요한 사실**: Lean4에서 `theorem`과 `lemma`는 기능적으로 **완전히 동일**하다!

```lean
-- 이 둘은 기능이 완전히 같다:
theorem my_theorem : 1 + 1 = 2 := by norm_num
lemma my_lemma : 1 + 1 = 2 := by norm_num
```

차이는 오직 **의도**(intention)에 있다:

| 키워드 | 의도 | 예시 |
|--------|------|------|
| `theorem` | 중요한 최종 결과 | "모든 짝수는 2로 나누어진다" |
| `lemma` | 다른 증명을 돕기 위한 보조 사실 | "n이 짝수이면 n = 2k인 k가 존재한다" |

### 7A.4.3 보조정리를 사용하는 패턴

```lean
-- 보조정리: 0은 모든 수의 배수이다
lemma zero_dvd_by_all (n : ℕ) : n ∣ 0 := dvd_zero n

-- 정리: 0은 3의 배수이다 (보조정리를 활용)
theorem zero_is_multiple_of_three : (3 : ℕ) ∣ 0 := zero_dvd_by_all 3
```

여기서 `zero_dvd_by_all`이라는 보조정리를 먼저 증명하고, 이를 `zero_is_multiple_of_three` 정리에서 **재사용**했다. 이것이 보조정리의 핵심 역할이다.

### 7A.4.4 실전 예시: 단계별로 쌓아가기

```lean
-- 1단계 보조정리: 가분성의 반사성 (자기 자신은 자기를 나눈다)
lemma dvd_self (n : ℕ) : n ∣ n := dvd_refl n

-- 2단계 보조정리: 1은 모든 수를 나눈다
lemma one_dvd_all (n : ℕ) : 1 ∣ n := one_dvd n

-- 정리: 1은 자기 자신을 나눈다 (두 보조정리 모두 적용 가능)
theorem one_divides_one : (1 : ℕ) ∣ 1 := dvd_self 1
-- 또는: one_dvd_all 1도 가능
```

---

## 7A.5 **→**(조건문)와 **↔**(쌍조건문)의 차이

가분성의 성질을 증명하다 보면, "A이면 B이다"(→)와 "A일 필요충분조건은 B이다"(↔)를 구별해야 하는 상황이 자주 나온다. 이 두 개념의 차이를 확실히 이해하자.

### 7A.5.1 **→**(if, 조건문): 한 방향

`P → Q`는 "P이면 Q이다"라는 뜻이다. **한 방향**만 보장한다.

일상적 예시:
- "비가 오면(P) 우산을 쓴다(Q)" — 비가 오면 우산을 쓰지만, 우산을 쓴다고 비가 오는 것은 아니다 (양산일 수도 있다)
- "3 ∣ n이면(P) 6 ∣ 2n이다(Q)" — 3이 n을 나누면 6이 2n을 나누지만, 역은 따로 증명해야 한다

Lean4에서:

```lean
-- "a가 b를 나누면 a가 b+a를 나눈다"는 →(조건문)
-- 역이 반드시 성립하는 것은 아니다
example (a b : ℕ) : a ∣ b → a ∣ (b + a) := by
  intro h          -- h : a ∣ b를 가정한다
  exact dvd_add h (dvd_refl a)
```

코드를 한 줄씩 분석한다:

| 코드 | 의미 |
|------|------|
| `a ∣ b → a ∣ (b + a)` | "a가 b를 나누면, a가 b+a도 나눈다" |
| `intro h` | 가정 `a ∣ b`를 `h`라는 이름으로 도입 |
| `dvd_add h (dvd_refl a)` | "a ∣ b이고 a ∣ a이면 a ∣ (b+a)"라는 정리를 적용 |

### 7A.5.2 **↔**(if and only if, 쌍조건문): 양 방향

`P ↔ Q`는 "P일 **필요충분조건**(necessary and sufficient condition)은 Q이다"라는 뜻이다. 즉, **양 방향** 모두 성립한다:
- P → Q (P이면 Q이다: **충분조건**)
- Q → P (Q이면 P이다: **필요조건**)

일상적 예시:
- "삼각형이 정삼각형이다 ↔ 세 변의 길이가 모두 같다"
  - 정삼각형이면 세 변이 같고 (→), 세 변이 같으면 정삼각형이다 (←)

교재의 예시 (정리 3, 4.1절):
> a ≡ b (mod m) ↔ a mod m = b mod m

이것은 "a와 b가 모듈로 m으로 합동인 것"과 "a와 b를 m으로 나눈 나머지가 같은 것"이 **같은 말**이라는 뜻이다.

### 7A.5.3 Lean4에서 ↔ 다루기

```lean
-- "n이 짝수이다 ↔ 2가 n을 나눈다"
-- (Lean4에서 Even n은 ∃ k, n = 2 * k로 정의됨)
-- 이것을 직접 증명해 보자

example (n : ℕ) : Even n ↔ 2 ∣ n := by
  constructor                   -- 두 방향으로 나눈다
  · -- (→) 방향: Even n → 2 ∣ n
    intro ⟨k, hk⟩              -- Even n이면 n = 2 * k인 k가 존재
    exact ⟨k, by omega⟩        -- 따라서 2 ∣ n
  · -- (←) 방향: 2 ∣ n → Even n
    intro ⟨k, hk⟩              -- 2 ∣ n이면 n = 2 * k인 k가 존재
    exact ⟨k, by omega⟩        -- 따라서 Even n
```

코드를 한 줄씩 분석한다:

| 코드 | 의미 |
|------|------|
| `constructor` | 쌍조건문(↔)을 두 방향으로 나눈다 |
| `intro ⟨k, hk⟩` | 존재 명제를 분해한다 |
| `exact ⟨k, by omega⟩` | 증인 k를 제시하고, 등식을 omega로 확인 |

### 7A.5.4 ↔에서 한 방향만 꺼내기: `.mp`와 `.mpr`

이미 증명된 ↔ 정리에서 한 방향만 필요할 때:

| 표현 | 의미 | 기억법 |
|------|------|--------|
| `h.mp` | **정방향**(→) 추출 | mp = modus ponens |
| `h.mpr` | **역방향**(←) 추출 | mpr = modus ponens reverse |

```lean
-- 예시: 이미 증명된 정리에서 한 방향만 사용
-- even_iff_two_dvd : Even n ↔ 2 ∣ n 라는 정리가 있다고 하자

-- 정방향만 사용: Even n → 2 ∣ n
-- h.mp (여기서 h : Even n ↔ 2 ∣ n)

-- 역방향만 사용: 2 ∣ n → Even n
-- h.mpr
```

### 7A.5.5 ↔의 강력한 기능: `rw`에서 사용

**→**(조건문)과 **↔**(쌍조건문)의 실용적 차이가 가장 뚜렷하게 나타나는 곳이 `rw`(rewrite) 전술이다.

- **↔는 `rw`에서 사용 가능**: 양 방향이 보장되므로 치환 가능
- **→는 `rw`에서 사용 불가**: 한 방향만 알면 치환할 수 없다

```lean
-- ↔는 rw에서 사용 가능
-- 예: h : P ↔ Q 라면
-- rw [h]   → 목표의 P를 Q로 바꿈
-- rw [← h] → 목표의 Q를 P로 바꿈

-- →는 rw에서 사용 불가
-- 예: h : P → Q 라면
-- rw [h]   → ❌ 오류! (왜냐하면 역방향이 보장되지 않음)
```

### 7A.5.6 → vs ↔ 요약 비교표

| 특성 | → (조건문) | ↔ (쌍조건문) |
|------|-----------|-------------|
| 방향 | 한 방향 | 양 방향 |
| 읽는 법 | "P이면 Q" | "P일 필요충분조건은 Q" |
| 증명 방법 | `intro` | `constructor` → 각 방향 `intro` |
| 추출 | 직접 적용 | `.mp` (정방향), `.mpr` (역방향) |
| `rw` 사용 | ❌ 불가 | ✅ 가능 |
| 수학 기호 | ⇒ | ⇔ |
| Lean4 기호 | `→` (\to 또는 \r) | `↔` (\iff 또는 \lr) |

---

## 7A.6 교재 예제 1: 기본 가분성 확인

### 7A.6.1 교재 원문

> **예제 1** (Rosen 4.1절): 3 ∣ 7인지, 3 ∣ 12인지 알아보자.
> 
> 풀이: 3 ∤ 7임을 알 수 있는데, 7/3은 정수가 아니기 때문이다.  
> 또한, 3 ∣ 12임을 알 수 있는데 12/3 = 4이기 때문이다.

### 7A.6.2 Lean4 코드 (완성된 예제)

```lean
-- 예제 1a: 3은 7을 나누지 않는다
example : ¬ ((3 : ℕ) ∣ 7) := by norm_num

-- 예제 1b: 3은 12를 나눈다
example : (3 : ℕ) ∣ 12 := by norm_num
```

### 7A.6.3 단계별 설명

`norm_num`은 **수치 계산**(numerical normalization) 전술이다. 구체적인 숫자가 나올 때 자동으로 계산해 준다.

더 상세하게 증명하면:

```lean
-- 3 ∣ 12를 상세하게 증명
example : (3 : ℕ) ∣ 12 := by
  -- 12 = 3 × 4를 보이면 된다
  use 4          -- 증인(witness) c = 4를 제시
  -- 이제 목표: 12 = 3 * 4
  norm_num       -- 계산으로 확인
```

### 7A.6.4 중간 괄호 채우기 연습

```lean
-- 연습: 빈칸을 채워라
example : (3 : ℕ) ∣ 12 := by
  use _          -- 🔲 12 = 3 × ?  → 빈칸에 들어갈 수는?
```

<details>
<summary>💡 답 보기</summary>

```lean
example : (3 : ℕ) ∣ 12 := by
  use 4          -- 12 = 3 × 4
```

</details>

```lean
-- 연습: 빈칸을 채워라
example : (5 : ℕ) ∣ 25 := by
  use _          -- 🔲 25 = 5 × ?
```

<details>
<summary>💡 답 보기</summary>

```lean
example : (5 : ℕ) ∣ 25 := by
  use 5          -- 25 = 5 × 5
```

</details>

```lean
-- 연습: 빈칸을 채워라
example : ¬ ((3 : ℕ) ∣ 7) := by
  _              -- 🔲 어떤 전술을 쓸까?
```

<details>
<summary>💡 답 보기</summary>

```lean
example : ¬ ((3 : ℕ) ∣ 7) := by
  norm_num       -- 3은 7을 나누지 않음을 계산으로 확인
```

</details>

---

## 7A.7 교재 예제 2: n 이하의 양의 정수 중 d의 배수 개수

### 7A.7.1 교재 원문

> **예제 2** (Rosen 4.1절): n과 d가 양의 정수라고 하자. n 이하의 수 중 d의 배수는 몇 개나 있는가?
> 
> 풀이: d로 나누어 떨어지는 수는 dk꼴의 정수인데, 여기서 k는 양의 정수이다.  
> 따라서, n 이하의 수 중 d로 나누어 떨어지는 정수의 개수는 0 < dk ≤ n, 또는 0 < k ≤ n/d를 만족하는 k의 개수와 같다.  
> 따라서, n 이하의 양의 정수 중 ⌊n/d⌋개의 양의 정수가 d로 나누어 떨어진다.

### 7A.7.2 Lean4에서 바닥 함수(floor)

Lean4에서 자연수의 나눗셈은 **자동으로 바닥 함수**가 적용된다. 즉, `n / d`는 항상 ⌊n/d⌋이다.

```lean
-- 자연수 나눗셈은 자동으로 내림
#eval 12 / 5    -- 결과: 2  (= ⌊12/5⌋ = ⌊2.4⌋)
#eval 7 / 3     -- 결과: 2  (= ⌊7/3⌋ = ⌊2.33...⌋)
#eval 15 / 5    -- 결과: 3  (= ⌊15/5⌋)
```

### 7A.7.3 구체적 계산 예시

```lean
-- 100 이하의 양의 정수 중 7의 배수의 개수
#eval 100 / 7   -- 결과: 14  (7, 14, 21, ..., 98)

-- 50 이하의 양의 정수 중 3의 배수의 개수
#eval 50 / 3    -- 결과: 16  (3, 6, 9, ..., 48)
```

---

## 7A.8 교재 정리 1: 가분성의 기본 성질

### 7A.8.1 교재 원문

> **정리 1** (Rosen 4.1절):  
> a, b, c가 a ≠ 0인 정수라고 하자. 그러면 다음이 성립한다.  
> (i) 만약 a ∣ b이고 a ∣ c이면 a ∣ (b + c)이다.  
> (ii) 만약 a ∣ b이면, 모든 정수 c에 대해 a ∣ bc이다.  
> (iii) 만약 a ∣ b이고 b ∣ c이면, a ∣ c이다.

이 정리의 세 부분은 각각 "가분성은 덧셈에 대해 보존된다", "가분성은 곱셈에 대해 보존된다", "가분성은 추이적이다"라는 의미이다.

### 7A.8.2 (i) 가분성과 덧셈: a ∣ b이고 a ∣ c이면 a ∣ (b + c)

**수학적 증명**:
- a ∣ b이므로 b = as인 정수 s가 존재한다.
- a ∣ c이므로 c = at인 정수 t가 존재한다.
- 따라서 b + c = as + at = a(s + t)
- s + t는 정수이므로, a ∣ (b + c)이다.

**Lean4 코드** (완성된 예제):

```lean
-- 정리 1(i): a ∣ b이고 a ∣ c이면 a ∣ (b + c)
theorem thm1_i (a b c : ℕ) (hab : a ∣ b) (hac : a ∣ c) : a ∣ (b + c) := by
  exact dvd_add hab hac
```

한 줄씩 분석:

| 코드 | 의미 |
|------|------|
| `(hab : a ∣ b)` | 가정: a가 b를 나눈다 |
| `(hac : a ∣ c)` | 가정: a가 c를 나눈다 |
| `: a ∣ (b + c)` | 결론: a가 b+c를 나눈다 |
| `dvd_add hab hac` | Mathlib의 정리: "두 수를 나누면 합도 나눈다" |

**더 상세한 증명** (존재 증인을 직접 구성):

```lean
-- 정리 1(i) — 상세 증명
theorem thm1_i_detailed (a b c : ℕ) (hab : a ∣ b) (hac : a ∣ c) : a ∣ (b + c) := by
  -- hab을 분해: b = a * s인 s가 존재
  obtain ⟨s, hs⟩ := hab
  -- hac을 분해: c = a * t인 t가 존재
  obtain ⟨t, ht⟩ := hac
  -- 증인을 제시: b + c = a * (s + t)
  use s + t
  -- hs : b = a * s, ht : c = a * t
  -- 목표: b + c = a * (s + t)
  rw [hs, ht]
  ring
```

한 줄씩 분석:

| 코드 | 의미 |
|------|------|
| `obtain ⟨s, hs⟩ := hab` | `hab : a ∣ b`를 분해하여 s와 hs : b = a * s를 얻는다 |
| `obtain ⟨t, ht⟩ := hac` | `hac : a ∣ c`를 분해하여 t와 ht : c = a * t를 얻는다 |
| `use s + t` | 증인 c = s + t를 제시한다 |
| `rw [hs, ht]` | b를 a*s로, c를 a*t로 치환한다 |
| `ring` | a*s + a*t = a*(s+t)를 산술적으로 확인한다 |

### 7A.8.3 중간 괄호 채우기 연습: 정리 1(i)

```lean
-- 연습: 빈칸을 채워라
theorem thm1_i_practice (a b c : ℕ) (hab : a ∣ b) (hac : a ∣ c) : a ∣ (b + c) := by
  obtain ⟨s, hs⟩ := hab
  obtain ⟨t, ht⟩ := _      -- 🔲 어떤 가정을 분해해야 할까?
  use _                     -- 🔲 증인은 무엇일까?
  rw [hs, ht]
  ring
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem thm1_i_practice (a b c : ℕ) (hab : a ∣ b) (hac : a ∣ c) : a ∣ (b + c) := by
  obtain ⟨s, hs⟩ := hab
  obtain ⟨t, ht⟩ := hac     -- hac를 분해한다
  use s + t                  -- 증인: s + t
  rw [hs, ht]
  ring
```

</details>

### 7A.8.4 (ii) 가분성과 곱셈: a ∣ b이면 모든 c에 대해 a ∣ bc

**수학적 증명**:
- a ∣ b이므로 b = as인 정수 s가 존재한다.
- 따라서 bc = (as)c = a(sc)
- sc는 정수이므로, a ∣ bc이다.

**Lean4 코드**:

```lean
-- 정리 1(ii): a ∣ b이면 a ∣ b * c
theorem thm1_ii (a b c : ℕ) (hab : a ∣ b) : a ∣ (b * c) := by
  exact dvd_mul_of_dvd_left hab c
```

**상세 증명**:

```lean
-- 정리 1(ii) — 상세 증명
theorem thm1_ii_detailed (a b c : ℕ) (hab : a ∣ b) : a ∣ (b * c) := by
  obtain ⟨s, hs⟩ := hab     -- b = a * s
  use s * c                  -- 증인: s * c
  rw [hs]                   -- b * c → (a * s) * c
  ring                      -- (a * s) * c = a * (s * c)
```

### 7A.8.5 중간 괄호 채우기 연습: 정리 1(ii)

```lean
-- 연습: 빈칸을 채워라
theorem thm1_ii_practice (a b c : ℕ) (hab : a ∣ b) : a ∣ (b * c) := by
  obtain ⟨s, hs⟩ := _       -- 🔲 어떤 가정?
  use _                     -- 🔲 증인은?
  rw [hs]
  ring
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem thm1_ii_practice (a b c : ℕ) (hab : a ∣ b) : a ∣ (b * c) := by
  obtain ⟨s, hs⟩ := hab     -- hab을 분해
  use s * c                  -- 증인: s * c
  rw [hs]
  ring
```

</details>

### 7A.8.6 (iii) 가분성의 추이성: a ∣ b이고 b ∣ c이면 a ∣ c

**수학적 증명**:
- a ∣ b이므로 b = as인 정수 s가 존재한다.
- b ∣ c이므로 c = bt인 정수 t가 존재한다.
- 따라서 c = bt = (as)t = a(st)
- st는 정수이므로, a ∣ c이다.

**Lean4 코드**:

```lean
-- 정리 1(iii): a ∣ b이고 b ∣ c이면 a ∣ c (추이성)
theorem thm1_iii (a b c : ℕ) (hab : a ∣ b) (hbc : b ∣ c) : a ∣ c := by
  exact dvd_trans hab hbc
```

**상세 증명**:

```lean
-- 정리 1(iii) — 상세 증명
theorem thm1_iii_detailed (a b c : ℕ) (hab : a ∣ b) (hbc : b ∣ c) : a ∣ c := by
  obtain ⟨s, hs⟩ := hab     -- b = a * s
  obtain ⟨t, ht⟩ := hbc     -- c = b * t
  use s * t                  -- 증인: s * t
  rw [ht, hs]               -- c = b * t = (a * s) * t
  ring                      -- (a * s) * t = a * (s * t)
```

### 7A.8.7 중간 괄호 채우기 연습: 정리 1(iii)

```lean
-- 연습: 빈칸을 채워라
theorem thm1_iii_practice (a b c : ℕ) (hab : a ∣ b) (hbc : b ∣ c) : a ∣ c := by
  obtain ⟨s, hs⟩ := hab
  obtain ⟨t, ht⟩ := hbc
  use _                     -- 🔲 증인은?
  rw [_, _]                 -- 🔲 어떤 것을 치환? (두 개)
  ring
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem thm1_iii_practice (a b c : ℕ) (hab : a ∣ b) (hbc : b ∣ c) : a ∣ c := by
  obtain ⟨s, hs⟩ := hab
  obtain ⟨t, ht⟩ := hbc
  use s * t                  -- 증인: s * t
  rw [ht, hs]               -- c를 b*t로, b를 a*s로 치환
  ring
```

</details>

---

## 7A.9 교재 따름정리 1: 선형 결합의 가분성

### 7A.9.1 교재 원문

> **따름정리 1** (Rosen 4.1절):  
> a ≠ 0인 세 정수 a, b, c가 a ∣ b와 a ∣ c를 만족하면,  
> 어떤 정수 m과 n에 대해서도 a ∣ mb + nc가 성립한다.

### 7A.9.2 수학적 증명

이것은 정리 1의 (ii)와 (i)를 결합한 것이다:
1. a ∣ b이므로, 정리 1(ii)에 의해 a ∣ mb
2. a ∣ c이므로, 정리 1(ii)에 의해 a ∣ nc
3. a ∣ mb이고 a ∣ nc이므로, 정리 1(i)에 의해 a ∣ (mb + nc)

### 7A.9.3 Lean4 코드

```lean
-- 따름정리 1: a ∣ b이고 a ∣ c이면 a ∣ (m * b + n * c)
theorem corollary1 (a b c m n : ℕ) (hab : a ∣ b) (hac : a ∣ c) :
    a ∣ (m * b + n * c) := by
  apply dvd_add                  -- 합을 나누려면 각각을 나눈다
  · exact dvd_mul_of_dvd_right hab m   -- a ∣ b → a ∣ m * b
  · exact dvd_mul_of_dvd_right hac n   -- a ∣ c → a ∣ n * c
```

**상세 증명** (보조정리를 명시적으로 사용):

```lean
-- 따름정리 1 — 상세 증명
-- 먼저 보조정리: a ∣ b → a ∣ m * b
lemma dvd_mul_left_of_dvd (a b m : ℕ) (h : a ∣ b) : a ∣ (m * b) := by
  obtain ⟨k, hk⟩ := h          -- b = a * k
  use m * k                     -- 증인: m * k
  rw [hk]; ring

-- 이제 따름정리를 보조정리를 사용해서 증명
theorem corollary1_detailed (a b c m n : ℕ) (hab : a ∣ b) (hac : a ∣ c) :
    a ∣ (m * b + n * c) := by
  have h1 : a ∣ (m * b) := dvd_mul_left_of_dvd a b m hab
  have h2 : a ∣ (n * c) := dvd_mul_left_of_dvd a c n hac
  exact dvd_add h1 h2
```

여기서 `have`라는 새로운 전술이 등장했다. `have`는 **중간 결과를 선언**하는 전술이다:

| 코드 | 의미 |
|------|------|
| `have h1 : a ∣ (m * b) := ...` | "a ∣ (m * b)라는 사실을 h1이라 부르자, 그 증명은 ..." |

이것이 바로 **보조정리**(lemma)를 활용하는 전형적인 패턴이다!

### 7A.9.4 sorry로 직접 풀기

```lean
-- 연습: sorry를 실제 증명으로 교체하라
theorem corollary1_exercise (a b c m n : ℕ) (hab : a ∣ b) (hac : a ∣ c) :
    a ∣ (m * b + n * c) := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem corollary1_exercise (a b c m n : ℕ) (hab : a ∣ b) (hac : a ∣ c) :
    a ∣ (m * b + n * c) := by
  apply dvd_add
  · exact dvd_mul_of_dvd_right hab m
  · exact dvd_mul_of_dvd_right hac n
```

</details>

---

## 7A.10 교재 정리 2: **나눗셈 알고리즘**(Division Algorithm)

### 7A.10.1 교재 원문

> **정리 2** (나눗셈 알고리즘):  
> a가 정수이고 d가 양의 정수라고 하자. 그러면 a = dq + r이 되는 유일한 정수 q와 r이 있는데,  
> 0 ≤ r < d이다.

이것은 매우 기본적이고 중요한 정리이다! 모든 정수를 양의 정수로 나누면, 반드시 **몫**(quotient) q와 **나머지**(remainder) r이 유일하게 존재하며, 나머지는 0 이상 d 미만이라는 것이다.

### 7A.10.2 교재의 정의 2: 용어 정리

> **정의 2**: 나눗셈 알고리즘에 있는 식에서  
> d는 **약수**(divisor)라고 a는 **나뉠수**(dividend)라고 불린다.  
> q는 **몫**(quotient), r은 **나머지**(remainder)라고 불린다.  
> 이 표기법으로 몫과 나머지를 표현한다:  
> q = a div d, r = a mod d

### 7A.10.3 Lean4에서의 표기

| 수학 표기 | Lean4 표기 | 의미 |
|---------|-----------|------|
| a div d | `a / d` | 몫 (자연수에서 자동 내림) |
| a mod d | `a % d` | 나머지 |

```lean
-- 자연수에서의 나눗셈
#eval 101 / 11    -- 결과: 9   (몫)
#eval 101 % 11    -- 결과: 2   (나머지)
-- 확인: 101 = 11 × 9 + 2 ✓

#eval 17 / 3      -- 결과: 5   (몫)
#eval 17 % 3      -- 결과: 2   (나머지)
-- 확인: 17 = 3 × 5 + 2 ✓
```

### 7A.10.4 나눗셈 알고리즘을 Lean4로 검증

```lean
-- 나눗셈 알고리즘: a = d * (a / d) + a % d
-- Lean4에서 이것은 정리 Nat.div_add_mod로 알려져 있다

example : 101 = 11 * (101 / 11) + 101 % 11 := by norm_num

-- 일반적인 형태:
example (a d : ℕ) (hd : 0 < d) : a = d * (a / d) + a % d := by
  exact (Nat.div_add_mod a d).symm
```

### 7A.10.5 교재 예제 3: 101을 11로 나누기

> **예제 3**: 101을 11로 나누었을 때 몫과 나머지는 얼마인가?
> 풀이: 101 = 11 · 9 + 2  
> 따라서, 몫은 9 = 101 div 11이고, 나머지는 2 = 101 mod 11이다.

```lean
-- 예제 3
example : 101 / 11 = 9 := by norm_num
example : 101 % 11 = 2 := by norm_num
example : 101 = 11 * 9 + 2 := by norm_num
```

### 7A.10.6 교재 예제 4: -11을 3으로 나누기

교재에서는 음수의 나눗셈도 다루지만, 자연수에서는 음수가 없으므로 이 예제는 정수(ℤ)로 다룬다.

> **예제 4**: -11을 3으로 나누었을 때 몫과 나머지는 얼마인가?
> 풀이: -11 = 3(-4) + 1  
> 따라서 몫은 -4이고 나머지는 1이다.

**주의**: 나머지는 항상 0 이상이어야 한다! -11 = 3(-3) - 2로 쓰면 나머지가 -2가 되어 조건을 만족하지 않는다.

```lean
-- 예제 4 (정수에서)
-- Lean4의 정수 나눗셈은 약간 다를 수 있으므로 직접 확인
#eval (-11 : Int) / 3     -- 결과 확인
#eval (-11 : Int) % 3     -- 결과 확인
```

### 7A.10.7 연습문제: 나눗셈 계산

```lean
-- 연습: 빈칸을 채워라
example : 44 / 8 = _ := by norm_num   -- 🔲
example : 44 % 8 = _ := by norm_num   -- 🔲
```

<details>
<summary>💡 답 보기</summary>

```lean
example : 44 / 8 = 5 := by norm_num   -- 44 = 8 × 5 + 4
example : 44 % 8 = 4 := by norm_num
```

</details>

```lean
-- 연습: 빈칸을 채워라
example : 777 / 21 = _ := by norm_num   -- 🔲
example : 777 % 21 = _ := by norm_num   -- 🔲
```

<details>
<summary>💡 답 보기</summary>

```lean
example : 777 / 21 = 37 := by norm_num   -- 777 = 21 × 37 + 0
example : 777 % 21 = 0 := by norm_num     -- 21이 777을 나눈다!
```

</details>

---

## 7A.11 **치환**(Substitution)과 `rw` 전술 복습

가분성 증명에서 `rw`(rewrite)는 매우 자주 사용된다. 여기서 한 번 더 복습하자.

### 7A.11.1 `rw`의 기본 원리

`rw`는 **등식**(=)이나 **쌍조건문**(↔)을 사용하여, 목표(goal)나 가정(hypothesis)에서 **한쪽을 다른 쪽으로 바꾸는** 전술이다.

```lean
-- h : a = b 일 때
-- rw [h]   → 목표에서 a를 b로 바꿈
-- rw [← h] → 목표에서 b를 a로 바꿈 (역방향)
-- rw [h] at hyp → 가정 hyp에서 a를 b로 바꿈
```

### 7A.11.2 가분성 증명에서의 `rw` 사용

```lean
-- 예: a ∣ b이고 b = 2 * a이면 a ∣ (2 * a)
example (a b : ℕ) (hab : a ∣ b) (heq : b = 2 * a) : a ∣ (2 * a) := by
  -- hab : a ∣ b인데, b = 2 * a이므로
  rw [heq] at hab     -- hab이 a ∣ (2 * a)로 바뀜
  exact hab
```

### 7A.11.3 `rw`와 `↔`

```lean
-- ↔는 rw에서 사용 가능 (→는 불가!)
-- 예시: Nat.dvd_iff_mod_eq_zero를 활용
example (a b : ℕ) (h : a % b = 0) (hb : b ≠ 0) : b ∣ a := by
  rwa [Nat.dvd_iff_mod_eq_zero]
```

여기서 `rwa`는 `rw` + `assumption`의 합성이다. `rw`로 치환한 후 목표가 가정과 같으면 자동으로 닫는다.

---

## 7A.12 연습 세트 1: 가분성 기초

### 연습 7A.1: 기본 가분성 판정 (중간 괄호)

```lean
-- 빈칸을 채워라: 4는 20을 나눈다
example : (4 : ℕ) ∣ 20 := by
  use _     -- 🔲 20 = 4 × ?
```

<details>
<summary>💡 답 보기</summary>

```lean
example : (4 : ℕ) ∣ 20 := by
  use 5     -- 20 = 4 × 5
```

</details>

### 연습 7A.2: 비가분성 판정

```lean
-- sorry를 채워라: 4는 15를 나누지 않는다
example : ¬ ((4 : ℕ) ∣ 15) := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
example : ¬ ((4 : ℕ) ∣ 15) := by
  norm_num
```

</details>

### 연습 7A.3: 정리 1(i) 응용 — 구체적 수

```lean
-- sorry를 채워라: 5 ∣ 10이고 5 ∣ 15이면 5 ∣ 25
example : (5 : ℕ) ∣ 10 → (5 : ℕ) ∣ 15 → (5 : ℕ) ∣ 25 := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
example : (5 : ℕ) ∣ 10 → (5 : ℕ) ∣ 15 → (5 : ℕ) ∣ 25 := by
  intro h1 h2
  exact dvd_add h1 h2
```

또는 더 간단하게:

```lean
example : (5 : ℕ) ∣ 10 → (5 : ℕ) ∣ 15 → (5 : ℕ) ∣ 25 := by
  intro h1 h2; norm_num
```

</details>

### 연습 7A.4: 정리 1(ii) 응용 — 일반적

```lean
-- sorry를 채워라: a ∣ b이면 a ∣ 3 * b
theorem dvd_three_mul (a b : ℕ) (h : a ∣ b) : a ∣ 3 * b := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem dvd_three_mul (a b : ℕ) (h : a ∣ b) : a ∣ 3 * b := by
  exact dvd_mul_of_dvd_right h 3
```

또는 상세 증명:

```lean
theorem dvd_three_mul' (a b : ℕ) (h : a ∣ b) : a ∣ 3 * b := by
  obtain ⟨k, hk⟩ := h
  use 3 * k
  rw [hk]; ring
```

</details>

### 연습 7A.5: 정리 1(iii) 응용 — 추이성

```lean
-- sorry를 채워라: 2 ∣ 4이고 4 ∣ 12이면 2 ∣ 12
example : (2 : ℕ) ∣ 4 → (4 : ℕ) ∣ 12 → (2 : ℕ) ∣ 12 := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
example : (2 : ℕ) ∣ 4 → (4 : ℕ) ∣ 12 → (2 : ℕ) ∣ 12 := by
  intro h1 h2
  exact dvd_trans h1 h2
```

</details>

### 연습 7A.6: 나눗셈 알고리즘 확인

```lean
-- sorry를 채워라: 123을 19로 나눈 몫과 나머지
-- (힌트: #eval 123 / 19와 #eval 123 % 19로 먼저 확인)
example : 123 / 19 = _ ∧ 123 % 19 = _ := by   -- 🔲 빈칸 채우기
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
example : 123 / 19 = 6 ∧ 123 % 19 = 9 := by
  constructor <;> norm_num
-- 확인: 123 = 19 × 6 + 9
```

</details>

### 연습 7A.7: `x ∣ x²` 증명 (sorry)

```lean
-- sorry를 채워라: 모든 자연수 x에 대해 x ∣ x²
-- (힌트: x² = x * x이다)
example (x : ℕ) : x ∣ x ^ 2 := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
example (x : ℕ) : x ∣ x ^ 2 := by
  use x
  ring
```

또는:

```lean
example (x : ℕ) : x ∣ x ^ 2 := by
  exact dvd_mul_left x x
```

</details>

### 연습 7A.8: 복합 증명 (sorry)

```lean
-- sorry를 채워라: x ∣ w이면 x ∣ (y * (x * z) + x ^ 2 + w ^ 2)
-- (Mathematics in Lean 예제)
example (x y z w : ℕ) (h : x ∣ w) : x ∣ y * (x * z) + x ^ 2 + w ^ 2 := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
example (x y z w : ℕ) (h : x ∣ w) : x ∣ y * (x * z) + x ^ 2 + w ^ 2 := by
  apply dvd_add
  apply dvd_add
  · -- x ∣ y * (x * z)
    apply dvd_mul_of_dvd_right
    apply dvd_mul_right
  · -- x ∣ x ^ 2
    exact dvd_mul_left x x
  · -- x ∣ w ^ 2
    exact dvd_pow h (by norm_num : 2 ≠ 0)
```

</details>

---

## 7A.13 Lean4에서 자주 쓰는 **가분성 관련 정리** 모음

아래 정리들은 Lean4/Mathlib에서 제공하는 것들이다. 이름을 기억해 두면 증명할 때 유용하다.

| 정리 이름 | 의미 | 예시 |
|---------|------|------|
| `dvd_refl a` | a ∣ a | 5 ∣ 5 |
| `dvd_zero a` | a ∣ 0 | 3 ∣ 0 |
| `one_dvd a` | 1 ∣ a | 1 ∣ 7 |
| `dvd_add h₁ h₂` | a ∣ b이고 a ∣ c이면 a ∣ (b + c) | |
| `dvd_mul_left a b` | a ∣ b * a | 3 ∣ 5 * 3 |
| `dvd_mul_right a b` | a ∣ a * b | 3 ∣ 3 * 5 |
| `dvd_mul_of_dvd_left h c` | a ∣ b이면 a ∣ b * c | |
| `dvd_mul_of_dvd_right h c` | a ∣ b이면 a ∣ c * b | |
| `dvd_trans h₁ h₂` | a ∣ b이고 b ∣ c이면 a ∣ c | |
| `dvd_pow h hn` | a ∣ b이면 a ∣ b ^ n (n ≠ 0) | |
| `Nat.dvd_iff_mod_eq_zero` | d ∣ n ↔ n % d = 0 | |

---

## 7A.14 도전 문제

### 도전 7A.1: 교재 연습문제 5

> **문제**: a와 b가 정수일 때 a ∣ b이고 b ∣ a이면, a = b 또는 a = -b임을 증명하라.

자연수에서는 더 간단하다: a ∣ b이고 b ∣ a이면 a = b.

```lean
-- 도전: a ∣ b이고 b ∣ a이면 a = b (자연수에서)
theorem dvd_antisymm_nat (a b : ℕ) (h1 : a ∣ b) (h2 : b ∣ a) : a = b := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem dvd_antisymm_nat (a b : ℕ) (h1 : a ∣ b) (h2 : b ∣ a) : a = b := by
  exact Nat.dvd_antisymm h1 h2
```

</details>

### 도전 7A.2: 교재 연습문제 6

> **문제**: a ≠ 0이고 a ∣ c, b ∣ d가 있다면, ab ∣ cd임을 증명하라.

```lean
-- 도전: a ∣ c이고 b ∣ d이면 a * b ∣ c * d
theorem dvd_mul_dvd (a b c d : ℕ) (hac : a ∣ c) (hbd : b ∣ d) : a * b ∣ c * d := by
  sorry
```

<details>
<summary>💡 답 보기</summary>

```lean
theorem dvd_mul_dvd (a b c d : ℕ) (hac : a ∣ c) (hbd : b ∣ d) : a * b ∣ c * d := by
  obtain ⟨s, hs⟩ := hac   -- c = a * s
  obtain ⟨t, ht⟩ := hbd   -- d = b * t
  use s * t
  rw [hs, ht]; ring
```

</details>

---

## 7A.15 전술 요약

### 새로운 전술 & 개념

| 전술/개념 | 용도 | 예시 |
|---------|------|------|
| `∣` | 가분성 기호 (\| 로 입력) | `a ∣ b` |
| `use` | 존재 증인 제시 | `use 4` |
| `obtain ⟨k, hk⟩ := h` | 존재 가정 분해 | `obtain ⟨s, hs⟩ := hab` |
| `have` | 중간 결과 선언 | `have h1 : a ∣ b := ...` |
| `dvd_add` | 가분성 합 규칙 | `dvd_add h1 h2` |
| `dvd_trans` | 가분성 추이성 | `dvd_trans h1 h2` |
| `dvd_refl` | 가분성 반사성 | `dvd_refl a` |
| `norm_num` | 수치 계산 자동화 | `norm_num` |
| `↔` | 쌍조건문 | `P ↔ Q` |
| `constructor` | ↔를 두 방향으로 분리 | |
| `.mp` / `.mpr` | ↔의 정방향/역방향 추출 | `h.mp`, `h.mpr` |

### 이전 장 전술 (복습)

| 전술 | 최초 등장 |
|------|---------|
| `intro`, `exact`, `apply` | Part 4 |
| `rw`, `simp`, `ring` | Part 4-5 |
| `omega` | Part 4 |

---

## 7A.16 핵심 정리 요약

1. **가분성**: a ∣ b ↔ ∃ c, b = a * c (b는 a의 배수)

2. **정리 1의 세 가지 성질**:
   - (i) a ∣ b, a ∣ c → a ∣ (b + c) — **합에 대한 보존**
   - (ii) a ∣ b → a ∣ bc — **곱에 대한 보존**
   - (iii) a ∣ b, b ∣ c → a ∣ c — **추이성**

3. **나눗셈 알고리즘**: a = d × (a / d) + (a % d), 단 0 ≤ a % d < d

4. **정리 vs 보조정리**: Lean4에서 기능 동일, 의도만 다름

5. **→ vs ↔**: → 한 방향, ↔ 양 방향. ↔만 `rw`에서 사용 가능

---

## 다음 편(7-B) 예고

**제7-B편**에서는 교재 4.1절의 나머지 부분을 다룬다:
- **합동**(congruence): a ≡ b (mod m)
- **합동의 성질**: 합과 곱이 합동을 유지하는 것
- **나머지 산술**(modular arithmetic): ℤ_m에서의 연산
- 교재의 예제 5, 6, 7, 8과 연습문제

를 Lean4로 구현한다.

---

**(끝)**
